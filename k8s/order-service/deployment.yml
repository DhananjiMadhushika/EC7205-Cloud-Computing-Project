apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecommerce-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: order-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3002
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: ecommerce-config
              key: NODE_ENV
        - name: MONGODB_CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: ecommerce-secrets
              key: MONGODB_CONNECTION_STRING_ORDER
        - name: RABBITMQ_URL
          valueFrom:
            configMapKeyRef:
              name: ecommerce-config
              key: RABBITMQ_URL
        livenessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3002
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce-app
spec:
  selector:
    app: order-service
  ports:
  - port: 3002
    targetPort: 3002